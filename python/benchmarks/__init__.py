"""Evaluation harness for bangla-ai backends.

Not part of the published package — lives alongside the source for development
and CI. Run with ``python -m benchmarks.run`` from the ``python/`` directory.
"""
